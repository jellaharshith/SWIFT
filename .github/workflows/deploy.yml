# DISABLED: Backend migrated to Railway (auto-deploys via GitHub integration).
# Railway project: https://railway.app (connect repo in Railway dashboard)
# This file is kept for historical reference only.

name: Deploy (Disabled - Using Railway)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Deployment is handled by Railway GitHub integration. See railway.json."
